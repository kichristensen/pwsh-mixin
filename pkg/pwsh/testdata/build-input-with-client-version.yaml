config:
  clientVersion: v0.0.0
install:
  - pwsh:
      description: "Summon Minion"
      arguments:
      - "man-e-faces"
      flags:
        species: "human"
      outputs:
        - name: "VICTORY"
          jsonPath: "$Id"
